<Window x:Class="Wpf_caculator.History"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Wpf_caculator"
        mc:Ignorable="d"
        Title="History" Height="450" Width="800" Loaded="LoadDate" WindowStartupLocation="CenterScreen">
    <Grid>
        <DataGrid x:Name="dgvListStuden"
                  CanUserAddRows="False"
                  AlternatingRowBackground="AntiqueWhite"
                  AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Content"
                                    Binding="{Binding Content}"
                                    Width="*" />
                <DataGridTextColumn Header="Log"
                                    Binding="{Binding Log}"
                                    Width="*" />

            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
